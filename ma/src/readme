2-16：现在卡死在pt加不进去对象，因为第一个对象在窗口左边界，窗口滑动不了，现在要滑动到窗口对象的第二个位置
2-17：滑动窗口部分调试通过。
2-20：后分开编写的时候会报错多次定义，原因是全局变量和函数没有头文件，直接在main.cpp中引用会报这个错误。重新定义
头文件之后就好了。----------有extern不要引入头文件，头文件和源文件同一个名字的时候，cpp不用引入h。
2-23：发现是main.cpp和新建的对应cpp都引用了对应h文件，导致的重复定义。


powerlaw length分布

